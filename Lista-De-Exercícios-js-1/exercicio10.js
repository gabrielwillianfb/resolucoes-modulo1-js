// Exercício 10 da lista de exercícios passada na aula 06.

const prompt = require("prompt-sync")();

const valor = parseInt(prompt("Digite um número: "));

for (let i = 0; i < 10; i++) {
	console.log(valor);
}
